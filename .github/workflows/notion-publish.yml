name: Publier ressource depuis Notion

on:
  workflow_dispatch:
    inputs:
      page_id:
        description: 'ID de la page Notion à publier ou dépublier'
        required: true
        type: string
      slug:
        description: 'Slug de la ressource'
        required: true
        type: string

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Installer les dépendances
        run: npm ci

      - name: Synchroniser ressource Notion
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          PAGE_ID: ${{ github.event.inputs.page_id }}
          SLUG: ${{ github.event.inputs.slug }}
        run: |
          node scripts/download_resource.mjs "$PAGE_ID" "$SLUG"

      - name: Commit et push des changements
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A public/ressources/ data/ressources/
          git diff --cached --quiet || (git commit -m "chore(notion): sync ressource ${{ github.event.inputs.slug }}" && git push origin main)

      - name: Déployer sur Vercel
        if: ${{ env.VERCEL_TOKEN != '' }}
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          npx vercel --prod --token "$VERCEL_TOKEN" --yes
